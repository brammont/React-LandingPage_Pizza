import React from 'react'
import {
    HeroContainer,
    HeroContent,
    
  } from './HeroElements';
import Navbar from '../Navbar';

const Hero = () => {
    return (
        <HeroContainer>
            <Navbar/>
            <HeroContent>
                <h1>fd</h1>
            </HeroContent>
        </HeroContainer>

    );
};

export default Hero
