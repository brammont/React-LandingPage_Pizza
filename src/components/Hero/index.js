import React from 'react'
import {
    HeroContainer,
    
  } from './HeroElements';
import Navbar from '../Navbar';

const Hero = () => {
    return (
        <HeroContainer>
            <Navbar/>
        </HeroContainer>

    );
};

export default Hero
